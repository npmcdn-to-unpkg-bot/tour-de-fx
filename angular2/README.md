# Angular 2 - Todo App example - Beta 10

This is a todo app sample using the angular 2 framework, it also uses typescript.

To run this sample app, you need a static web server:

```
npm i w3 -g
w3
```

Open your browser on port 3000
